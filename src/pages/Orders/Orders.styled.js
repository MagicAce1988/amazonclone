import styled from "styled-components";

const colors = (props) => props.theme.colors.orders;

export const Container = styled.div`
  padding: 20px 80px;
  h1 {
    margin: 20px 0;
  }
`;

export const Order = styled.div``;
